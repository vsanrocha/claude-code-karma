# Public environment variables (prefixed with PUBLIC_ are available to the client)
# API base URL for the FastAPI backend (default: http://localhost:8000)
PUBLIC_API_URL=http://localhost:8000

# Private environment variables (only available server-side)
# DATABASE_URL=
